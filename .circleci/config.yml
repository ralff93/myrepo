version: 2
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
            ll -f
      - run:
          name: Code Has Arrived
          command: |
            ls -al
            ll
            echo '^^^That should look familiar^^^'
          when: on_fail
      - run:
          name: cod retray
          command: |
            ls -al
            echo '^^^run if errors^^^'
