version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run:
          name: FIRST STEP
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
            cat /etc/os-release
            id
            ls -la
