version: 2
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: 1 error
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
            ll -f
      - run:
          name: 2 error
          command: |
            ls -al
            ll
            echo '^^^Riki^^^'
            echo '^^^run if errors in step Heloo world^^^'
          when: on_fail
      - run:
          name: 3 not erors
          command: |
            ls -al
            echo '^^^run if errors on Riki^^^'
          when: on_fail
      - run:
          name: 4 not erors
          command: |
            ls -al
            echo '^^^run if errors on Riki^^^'
          when: on_fail
